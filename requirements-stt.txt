# CrisperWhisper 전용 venv(venv_stt)에만 설치하세요.
#
# GPU 없는 서버 권장 순서:
#   python3 -m venv venv_stt
#   source venv_stt/bin/activate
#   pip install --upgrade pip
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip uninstall -y ctranslate2 || true
#   pip install -r requirements-stt.txt
#
# 주의:
# - 일반 ctranslate2 를 깔면 ct2 포크와 충돌합니다. 반드시 uninstall 하세요.
# - pip install "crisperwhisper[ct2]" 는 Linux에서 nvidia CUDA 휠을 크게 받을 수 있어
#   디스크가 부족하면 아래처럼 포크만 따로 설치하는 방식을 씁니다.
#
# 메인 venv(spleeter/celery/gTTS)에 직접 설치하지 마세요.
crisperwhisper[transformers]>=2.0.0
librosa>=0.10.0
ctranslate2-crisperwhisper>=4.7.1.post3,<5
